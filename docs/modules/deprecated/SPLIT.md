# jUtils Documentation - The "split" module

## About
The jUtils "split" module returns a version of the provided string that has been split into its individual characters

## Usage
`jUtils.split(string: string): Array<string> | false` - Returns the characters of `string` if it is a string or `false` if there's an error
